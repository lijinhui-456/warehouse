# 端口号
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ck?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Ljh159357
    driver-class-name: com.mysql.cj.jdbc.Driver

# MyBatis-Plus配置
mybatis-plus:
  #  mapper文件位置
  mapper-locations: classpath:mapper/*.xml
  # 实体类包名
  type-aliases-package: com.example.warehouse.entity
  # 日志输出
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT配置，过期时间
jwt:
  expiration: 3600000  # 24小时，单位毫秒
